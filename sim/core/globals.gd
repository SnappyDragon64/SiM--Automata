extends Node

enum CURSOR_MODES {
	SELECT,
	CREATE,
	LINK,
	DELETE,
	MOVE,
	PAN,
}

var CURSOR_MODE : CURSOR_MODES = CURSOR_MODES.SELECT

enum STATE_STATUS {
	DEFAULT,
	VISITED,
	CURRENT
}

enum SIM_STATUS {
	RUN,
	TEST,
	ANIM_START,
	ANIM_END,
}
